Setup the graphite and grafana server

cd scripts
docker-compose up

Graphite: http://localhost
Grafana:  http://localhost:3000

On grafana, login with admin/admin. You need to change the password.


Start the application:

mvn spring-boot:run


Creating a backup from grafana

Restoring a backup in grafana